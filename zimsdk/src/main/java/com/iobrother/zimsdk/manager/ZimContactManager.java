package com.iobrother.zimsdk.manager;

import com.iobrother.zimsdk.ZimClient;

public class ZimContactManager {
    private ZimClient client;
    public ZimContactManager(ZimClient client) {
        this.client = client;
    }
}
